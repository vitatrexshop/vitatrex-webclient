name: 🚀 Deploy Web System → portal-v7x.vitatrexshop.com

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    name: Build & Deploy Admin Panel
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🏗️ Build Angular (production)
        run: npm run build:prod

      - name: 📤 Deploy to Hostinger via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          # تعديل اسم مجلد dist ليطابق اسم مشروع الـ Web System الخاص بك
          local-dir: ./dist/vitatrix_websystem/
          server-dir: /domains/portal-v7x.vitatrexshop.com/public_html/
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
